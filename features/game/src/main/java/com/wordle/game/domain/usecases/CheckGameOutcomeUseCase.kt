package com.wordle.game.domain.usecases

// CheckGameOutcomeUseCase — given the current row + guess result,
// returns Won, Lost, or Playing. Keeps that branching logic out of the ViewModel
class CheckGameOutcomeUseCase() {
}